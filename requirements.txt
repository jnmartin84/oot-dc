# Setup and compression
crunch64>=0.5.1,<1.0.0
ipl3checksum>=1.2.0,<2.0.0
pyyaml>=6.0.1,<7.0.0
pygfxd>=1.0.3,<2.0.0

# asm-differ
argcomplete
colorama
cxxfilt
python-Levenshtein
watchdog

# decomp-permuter
pycparser
toml

# tools
mapfile-parser>=2.3.5,<3.0.0
pyelftools==0.30
rabbitizer>=1.0.0,<2.0.0
spimdisasm>=1.28.1,<2.0.0

# Dreamcast AICA audio (sample transcoding). REQUIRED, not optional: without scipy
# tools/aica/transcode.py falls back to a stdlib decimate that produces wrong ADPCM
# (silent/broken sampled instruments). See tools/aica/transcode.py.
numpy
scipy
